<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="StudentTestingApp.View.QuestionPage">
    <ScrollView BackgroundColor="#616161">
        <StackLayout>
            <StackLayout.Resources>
                <ResourceDictionary>
                    <Style x:Key="QuestionPageTitle" TargetType="Label" BasedOn="{StaticResource Title}">
                        <Setter Property="BackgroundColor" Value="#616161"/>
                        <Setter Property="HorizontalTextAlignment" Value="Center"/>
                    </Style>
                </ResourceDictionary>
            </StackLayout.Resources>
            <Label x:Name="textLabel" Style="{StaticResource QuestionPageTitle}"/>
            <Image x:Name="image"/>
            <ListView x:Name="answersListView" Style="{StaticResource List}" ItemTapped="answerTapped">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Label Text="{Binding Text}" Style="{StaticResource ListItem}"/>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
                <ListView.Footer>
                    <StackLayout>
                        <Label Text="Ваш ответ" Style="{StaticResource QuestionPageTitle}"/>
                        <ListView x:Name="selectedAnswersListView" Style="{StaticResource List}" ItemTapped="selectedAnswerTapped">
                            <ListView.ItemTemplate>
                                <DataTemplate>
                                    <ViewCell>
                                        <Label Text="{Binding Text}" Style="{StaticResource ListItem}"/>
                                    </ViewCell>
                                </DataTemplate>
                            </ListView.ItemTemplate>
                        </ListView>
                    </StackLayout>
                </ListView.Footer>
            </ListView>
        </StackLayout>
    </ScrollView>
</ContentPage>